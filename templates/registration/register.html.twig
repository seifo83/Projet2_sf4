{% extends "_template.html.twig" %}

{% block title %}Inscription{% endblock %}

{% block content %}
  
    <div class="section">
        <h1 class="title">Inscription</h1>
    

        {{ form_start(registrationForm) }}
            {{ form_row(registrationForm.email, {label: "Adresse Email:"}) }}

            {{ form_row(registrationForm.pseudo, {label: "Pseudo:"}) }}

            {# {{ form_row(registrationForm.plainPassword, {
                label: 'Password'
            }) }} 
            cela est générer par maker le formulaire de base si on a besoin d'ajouter la confirmation de MDP
            on  refait le code 
            #}

            {{ form_row(registrationForm.plainPassword.first, {label: 'Mot de passe:'}) }}
            {{ form_row(registrationForm.plainPassword.second, {label: 'Confirmation votre Mot de passe:'}) }}


            <button type="submit" class="button is-primary is-fullwidth">S'inscrire</button>

        {{ form_end(registrationForm) }}

    </div>
{% endblock %}


